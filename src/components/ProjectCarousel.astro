---
// This file is no longer needed, logic moved to ProjectCarousel.jsx
// You can delete this file or keep it as a backup.

// Recibir los proyectos como props en lugar de obtenerlos aquí
const { projects = [] } = Astro.props;

// Limitar a 5 proyectos para el carrusel
const featuredProjects = projects.slice(0, 5);
---

<!-- HTML Structure moved to ProjectCarousel.jsx -->
<!-- Script logic moved to ProjectCarousel.jsx -->
<!-- Styles moved to ProjectCarousel.scss -->

<div class="project-carousel-placeholder">
  <!-- Placeholder content if needed, or just a comment -->
  <!-- ProjectCarousel functionality is now handled by the Preact component -->
</div> 